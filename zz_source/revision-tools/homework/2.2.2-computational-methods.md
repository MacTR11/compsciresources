# Homework — 2.2.2 Computational Methods

Name: ______________________   Date: __________   Mark: ______ / 20

*OCR H446 — set after the 2.2.2 lesson. Show traces/working.*

## Part A — This week's topic (~14 marks)

**1.** Complete the table by naming the **computational method** being used in each scenario. **[2]** *(AO1/AO2)*

| Scenario | Computational method |
|----------|----------------------|
| A weather service displays thousands of temperature readings as a colour-coded heat map instead of a long table of numbers. |   |
| A supermarket analyses millions of loyalty-card transaction records to discover unexpected patterns and relationships in shopping habits. |   |
| A delivery firm plans van routes with a rule of thumb that quickly produces a good-enough route rather than the guaranteed best one. |   |
| Before building a new ticketing website, engineers model how the proposed design will cope with the expected peak load. |   |

@@SPACE:0@@

**2.** Write the letter of the correct definition in the Match column for each term. Two definitions are not needed. **[2]** *(AO1)*

| Term | Match |
|------|-------|
| Decomposition |   |
| Divide and conquer |   |
| Abstraction |   |
| Problem recognition |   |

| Letter | Definition |
|--------|------------|
| A | Removing or hiding unnecessary detail to focus on what matters for the problem |
| B | Identifying and understanding a problem — its inputs, outputs and requirements — before attempting to solve it |
| C | Breaking a problem into smaller, more manageable sub-problems, which may each be different kinds of task |
| D | Repeatedly splitting a problem into smaller instances of the **same** problem, solving these, and combining the results where needed |
| E | A rule of thumb that finds a good-enough answer quickly without guaranteeing the best one |
| F | Searching very large data sets to find unexpected patterns and relationships |

@@SPACE:0@@

**3.** A photo-sharing website processes every uploaded photo in three stages — resize, apply filter, add watermark — with each stage handled by a separate processor. Photos P1, P2 and P3 arrive one after another.

(a) Complete the table to show which photo each stage is working on at each time step when the stages are **pipelined**. Use "—" for an idle stage. Time step 1 has been done for you. **[2]** *(AO2)*

| Time step | Resize | Apply filter | Add watermark |
|-----------|--------|--------------|---------------|
| 1 | P1 | — | — |
| 2 |   |   |   |
| 3 |   |   |   |
| 4 |   |   |   |
| 5 |   |   |   |

@@SPACE:0@@

(b) Using the table, explain why pipelining increases the **throughput** of this system. **[1]** *(AO2)*

**4.** A robot explores a maze. It follows a path until it reaches a dead end, then returns to the most recent junction and tries a different path, repeating this until it finds the exit.

(a) State the name of the problem-solving technique the robot is using. **[1]** *(AO1)*

(b) The five stages of the technique are listed below out of order. Number them 1–5. **[2]** *(AO2)*

| Stage | Order (1–5) |
|-------|-------------|
| Reach a dead end where no forward move is possible. |   |
| Choose a path at the first junction and start following it. |   |
| Try a different, untried path from that junction, continuing the exploration. |   |
| Continue extending the path, junction by junction, while forward moves are available. |   |
| Undo the most recent choice and return to the last junction that still has an untried option. |   |

@@SPACE:0@@

**5.** A delivery firm uses a **heuristic** to plan the routes for its vans rather than an exact method.

(a) State what is meant by a **heuristic**. **[1]** *(AO1)*

(b) Explain **why** a heuristic, rather than an exact algorithm, is appropriate for a route-planning problem such as the Travelling Salesman Problem. **[2]** *(AO2)*

**6.** State **one** feature of a problem that makes it possible to solve **computationally**. **[1]** *(AO1)*

## Part B — Retrieval: keep it fresh (~6 marks)

**7.** *(2.2.1)* State the defining difference between a **function** and a **procedure**. **[1]**

**8.** *(2.2.1)* A recursive subroutine has no reachable **base case**. State the run-time error that results and explain why it occurs. **[2]**

**9.** *(2.1.2)* State what is meant by **thinking ahead**, and give **two** examples of where it is used when designing a system. **[3]**

---

## Mark scheme

**Part A (14 marks)**

**1. [2]** — Two correct rows per mark:

| Scenario | Method |
|----------|--------|
| Heat map of temperature readings | **Visualisation** (visualisation to solve problems) |
| Loyalty-card records analysed for unexpected patterns | **Data mining** |
| Rule of thumb producing a good-enough route quickly | **Heuristics** |
| Modelling how a design copes with expected peak load | **Performance modelling** |

**2. [2]** — Two correct matches per mark:

| Term | Match |
|------|-------|
| Decomposition | C |
| Divide and conquer | D |
| Abstraction | A |
| Problem recognition | B |

Distractors not used: E (heuristic) and F (data mining).

**3.**

- (a) **[2]** — Two correct rows per mark:

| Time step | Resize | Apply filter | Add watermark |
|-----------|--------|--------------|---------------|
| 1 | P1 | — | — |
| 2 | P2 | P1 | — |
| 3 | P3 | P2 | P1 |
| 4 | — | P3 | P2 |
| 5 | — | — | P3 |

Each photo moves one stage to the right per time step; a new photo enters Resize as soon as it is free.

- (b) **[1]** — From time step 3 the stages work on **different photos at the same time**, so no stage sits idle waiting for a whole photo to finish all three stages — photos are completed at a faster **rate** (one per time step once the pipeline is full).

**4.**

- (a) **[1]** — **Backtracking**.
- (b) **[2]** — All five stages correct = 2 marks; three or four correct = 1 mark:

| Stage | Order |
|-------|-------|
| Reach a dead end | 3 |
| Choose a path at the first junction | 1 |
| Try a different, untried path from that junction | 5 |
| Continue extending the path while moves are available | 2 |
| Undo the most recent choice and return to the last junction with an untried option | 4 |

**5.**

- (a) **[1]** — A **heuristic** is a rule of thumb that produces a **good-enough** answer quickly, without guaranteeing the optimal answer.
- (b) **[2]** — 1 mark: an exact method must consider an enormous number of possible route orderings — the number of combinations grows so quickly with the number of stops that the problem becomes **intractable** (cannot be solved exactly in a reasonable time). 1 mark: a heuristic finds a **good-enough** route **quickly**, and a near-optimal route is acceptable in practice.

**6. [1]** — Any one: it can be **decomposed** into smaller sub-problems; it has identifiable **inputs, processes and outputs**; it can be solved by a **finite sequence of steps** (an algorithm); it is rule-based/repeatable; the data involved can be **modelled/structured**; useful **abstractions** can be made; it is tractable — solvable in a reasonable time.

**Part B (6 marks)**

**7. [1]** — A **function returns a value** (so a call can be used in an expression); a procedure does not return a value.

**8. [2]** — 1 mark: a **stack overflow**. 1 mark: with no reachable base case the subroutine keeps calling itself; each call pushes a new frame (return address/parameters/locals) onto the **call stack** until the stack's memory is exhausted.

**9. [3]** — 1 mark: **thinking ahead** is identifying in advance what a solution will need — its inputs and outputs, preconditions, and reusable components — before/while designing it. 1 mark per distinct example, max 2: deciding what data a form must capture before building it; **caching** data/resources likely to be needed again; specifying **preconditions** for a subroutine; planning reusable components/subroutines.

**Total: 14 + 6 = 20 marks**
