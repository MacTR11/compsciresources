# Homework — 2.1.3 Thinking Procedurally

Name: ______________________   Date: __________   Mark: ______ / 20

*OCR H446 — set after the 2.1.3 lesson.*

## Part A — This week's topic (~14 marks)

**1.** Write the letter of the correct definition in the Match column for each term. Two definitions are not needed. **[2]** *(AO1)*

| Term | Match |
|------|-------|
| Decomposition |   |
| Sub-procedure |   |
| Dependency |   |
| Independent steps |   |

| Letter | Definition |
|--------|------------|
| A | Breaking a complex problem down into smaller, more manageable sub-problems |
| B | Steps that do not use each other's outputs, so their relative order is not fixed |
| C | Hiding unnecessary detail so that only the important features of a problem remain |
| D | A named, self-contained block of code that performs one task and can be called whenever it is needed |
| E | Where one step needs the output of an earlier step, so the order of the two steps is fixed |
| F | Running two or more tasks at the same time on a multi-core processor |

@@SPACE:0@@

**2.** A washing-machine control program runs each of the six steps below exactly once, but they are listed out of order. Number the steps 1–6 so that every dependency is respected (for example, water cannot be heated before it is in the drum, and the drum cannot spin until the water has gone). **[3]** *(AO2)*

| Step | Order (1–6) |
|------|-------------|
| Spin |   |
| Lock door |   |
| Wash |   |
| Fill with water |   |
| Drain |   |
| Heat water |   |

@@SPACE:0@@

**3.** For each statement, write **True** or **False** in the middle column. If a statement is false, write a corrected version in the final column. **[4]** *(AO2)*

| Statement | True / False | Correction if false |
|-----------|--------------|---------------------|
| In a cinema-booking sequence `selectSeats` → `takePayment` → `issueTicket`, the step `issueTicket` could safely be moved before `takePayment`. |   |   |
| Decomposing a large open-world game into modules lets each programmer in a team develop and test a different module at the same time. |   |   |
| The washing machine also updates a countdown display; this update must always run after the wash step, because the order of every pair of tasks is fixed. |   |   |
| If `gradeAnswer` is written once as a sub-procedure and called for every quiz question, a fix to the grading code only has to be made in one place. |   |   |

@@SPACE:0@@

**4.** Complete the paragraph using words from the word bank. Three words are not needed. **[2]** *(AO1)*

When a problem is decomposed, each **(a)** __________ can be developed and tested separately. If one step uses the **(b)** __________ of an earlier step, there is a **(c)** __________ between the two steps and their order is **(d)** __________ .

**Word bank:** sub-problem · output · dependency · fixed · input · abstraction · concurrent

@@SPACE:0@@

**5.** A team is building an online cinema-ticket booking system. Identify **three** sub-procedures that the problem could be decomposed into. **[3]** *(AO2)*

## Part B — Retrieval: keep it fresh (~6 marks)

**6.** *(2.1.2)* State what is meant by a **precondition**, and give one example of a precondition for a routine that performs a **binary search**. **[2]** *(AO1/AO2)*

**7.** *(1.2.2)* State, in order, the **two** stages of compilation that come **before** code generation. **[2]** *(AO1)*

**8.** *(1.2.3)* State **one** advantage of the **Agile** development methodology over the **Waterfall** lifecycle model. **[2]** *(AO1)*

---

## Mark scheme

**Part A (14 marks)**

**1. [2]** — Two correct matches per mark:

| Term | Match |
|------|-------|
| Decomposition | A |
| Sub-procedure | D |
| Dependency | E |
| Independent steps | B |

Distractors not used: C (abstraction) and F (concurrent/parallel processing).

**2. [3]** — Two correctly numbered steps per mark:

| Step | Order |
|------|-------|
| Lock door | 1 |
| Fill with water | 2 |
| Heat water | 3 |
| Wash | 4 |
| Drain | 5 |
| Spin | 6 |

Each step needs the state produced by the one before it (water must be in the drum before heating, heated before washing, drained before spinning), so this is the only valid order.

**3. [4]** — 1 mark per row; for a false row the mark needs **False** plus a valid correction.

| Statement | True / False | Correction |
|-----------|--------------|------------|
| `issueTicket` could move before `takePayment` | **False** | `issueTicket` uses the **output** of `takePayment` (confirmation of successful payment), so it must stay after it — the order is fixed. |
| Decomposition lets team members build modules at the same time | **True** | — |
| The countdown-display update must always run after the wash step | **False** | The display update and the wash step do **not** use each other's outputs — there is no dependency, so their relative order is **not** fixed. |
| A sub-procedure means a fix is made in one place | **True** | — |

**4. [2]** — Two correct gaps per mark: (a) **sub-problem**; (b) **output**; (c) **dependency**; (d) **fixed**. (Distractors: input, abstraction, concurrent.)

**5. [3]** — Any three plausible sub-procedures (1 each): browse/select film and showing; check seat availability; allow the user to choose seats; take/validate payment; issue/email the ticket; update the seat-availability records; create the booking record. Must be plausible sub-tasks of the booking scenario — generic answers ("do the input", "do the processing") earn nothing.

**Part B (6 marks)**

**6. [2]** — 1 mark: a precondition is a condition that must be **true before** a routine runs for the routine to work correctly. 1 mark: for binary search, the list must already be **sorted** (accept: the list is not empty).

**7. [2]** — 1 mark each, in the correct order: **lexical analysis** (tokenising) then **syntax analysis** (parsing). (Code generation and optimisation follow.)

**8. [2]** — 1 mark + 1 mark for one developed advantage: Agile is **iterative**, delivering working software in increments and accommodating changing requirements **[1]**; the customer sees progress early and changes are cheaper to make, whereas Waterfall fixes requirements up front so late changes are costly **[1]**.

**Total: 14 + 6 = 20 marks**
