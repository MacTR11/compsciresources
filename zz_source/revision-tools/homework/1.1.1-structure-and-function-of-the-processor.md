# Homework — 1.1.1 Structure and Function of the Processor

Name: ______________________   Date: __________   Mark: ______ / 20

*OCR H446 — set after the 1.1.1 lesson. Show your working.*

## Part A — This week's topic (~14 marks)

**1.** Complete the table by filling in the **full name** and **role** of each register. **[4]** *(AO1)*

| Register | Full name | Role |
|---|---|---|
| PC | (a) | (b) |
| ACC | (c) | (d) |
| MAR | (e) | (f) |
| CIR | (g) | (h) |

@@SPACE:0@@

**2.** The events of the **fetch** and **decode** stages of the fetch–decode–execute cycle are shown below in a jumbled order. Number them **1–5** to show the correct order. **[2]** *(AO1)*

| Event | Order (1–5) |
|---|---|
| The instruction is transferred from memory to the MDR along the data bus |   |
| The Control Unit decodes the instruction in the CIR, splitting it into opcode and operand |   |
| The address held in the PC is copied into the MAR |   |
| The contents of the MDR are copied into the CIR |   |
| The PC is incremented by 1 |   |

@@SPACE:0@@

**3.** A processor uses the instruction set and memory contents below. The PC starts at **0** and the ACC starts at **0**. Complete the trace table, writing the value held in each column **after each instruction has finished executing**. The trace stops when HALT is fetched. **[3]** *(AO2)*

| Instruction | Meaning |
|---|---|
| LOAD n | Copy the contents of memory address n into the ACC |
| ADD n | Add the contents of memory address n to the ACC |
| STORE n | Copy the contents of the ACC into memory address n |
| HALT | Stop execution |

| Address | Contents |
|---|---|
| 0 | LOAD 4 |
| 1 | ADD 5 |
| 2 | STORE 6 |
| 3 | HALT |
| 4 | 7 |
| 5 | 5 |
| 6 | 0 |

| PC | CIR | ACC | Contents of address 6 |
|---|---|---|---|
|   |   |   |   |
|   |   |   |   |
|   |   |   |   |

@@SPACE:0@@

**4.** Complete the passage about processor performance using words from the word bank. Not all words are used. **[3]** *(AO1)*

Several factors affect the performance of a processor. Increasing the (a) __________ speed increases the number of fetch–decode–execute cycles completed each second. (b) __________ is a small amount of very fast memory built into or close to the CPU; it holds frequently used instructions and data so the processor makes fewer slow accesses to (c) __________. With (d) __________, the processor overlaps the stages of the cycle: while one instruction is being (e) __________, the next is being decoded and a third is being fetched. A processor with more than one (f) __________ can carry out several instructions truly in parallel.

**Word bank:** cache, clock, core, executed, interrupt, pipelining, RAM, register, ROM

@@SPACE:0@@

**5.** A processor in an embedded system has a **20-bit address bus** and an **8-bit data bus**.

**(a)** Calculate the maximum number of memory locations that can be directly addressed. Show your working. **[1]** *(AO2)*

**(b)** State **one** effect on the performance of the processor of widening the data bus to 16 bits. **[1]** *(AO1)*

## Part B — Retrieval: keep it fresh (~6 marks)

**6.** Match each bus to its description by writing the correct **letter** in the Match column. Two descriptions are not needed. **[3]** *(1.1.1)*

| Bus | Match |
|---|---|
| Address bus |   |
| Data bus |   |
| Control bus |   |

| Letter | Description |
|---|---|
| A | Bidirectional; carries data and instructions between the CPU and memory |
| B | Unidirectional; carries the address of the memory location to be accessed, from the CPU to memory |
| C | Bidirectional; carries signals such as read/write, clock and interrupt |
| D | Unidirectional; carries the results of calculations back to the ACC |
| E | Bidirectional; carries the address of the next instruction from memory to the CPU |

@@SPACE:0@@

**7.** Explain what **cache** memory is and **one** reason it improves CPU performance. **[2]** *(1.1.1)*

**8.** State **one** difference between the **Von Neumann** and **Harvard** architectures. **[1]** *(1.1.1)*

---

## Mark scheme

**Part A (14 marks)**

**1. [4]** — 2 correct cells per mark. Completed table:

| Register | Full name | Role |
|---|---|---|
| PC | Program Counter | Holds the address of the **next** instruction to be fetched (incremented each cycle) |
| ACC | Accumulator | Stores the immediate result of ALU calculations |
| MAR | Memory Address Register | Holds the address of the location in memory to be read from / written to |
| CIR | Current Instruction Register | Holds the instruction currently being decoded and executed |

**2. [2]** — Correct order:

| Event | Order |
|---|---|
| Instruction transferred from memory to the MDR along the data bus | 2 |
| CU decodes the instruction in the CIR into opcode and operand | 5 |
| Address held in the PC is copied into the MAR | 1 |
| Contents of the MDR are copied into the CIR | 4 |
| PC is incremented by 1 | 3 |

All five correctly placed = 2 marks; three or four correctly placed = 1 mark. *(Accept the PC increment at position 2, 3 or 4 provided the other events keep their relative order.)*

**3. [3]** — 1 mark per fully correct row:

| PC | CIR | ACC | Contents of address 6 |
|---|---|---|---|
| 1 | LOAD 4 | 7 | 0 |
| 2 | ADD 5 | 12 | 0 |
| 3 | STORE 6 | 12 | 12 |

*(Working: LOAD 4 copies 7 into ACC; ADD 5 gives 7 + 5 = 12; STORE 6 writes 12 to address 6. The PC is incremented during each fetch, so it reads 1, 2, 3.)*

**4. [3]** — 2 correct blanks per mark: (a) **clock**; (b) **cache**; (c) **RAM**; (d) **pipelining**; (e) **executed**; (f) **core**. *(Distractors not used: interrupt, register, ROM.)*

**5.**

**(a) [1]** — 2²⁰ = **1,048,576 locations**. *(Accept "1M" / "1 mega-location".)*

**(b) [1]** — More bits are carried per transfer, so more data/instructions move per cycle → higher throughput / faster data movement.

**Part B (6 marks)**

**6. [3]** — 1 mark per row: Address bus = **B**; Data bus = **A**; Control bus = **C**. *(D and E are distractors.)*

**7. [2]** — 1 mark: cache is **small, very fast memory on/near the CPU** holding frequently/recently used data/instructions (not part of RAM). 1 mark: it reduces the need to fetch from **slower RAM**, so data is retrieved faster / fewer slow main-memory accesses.

**8. [1]** — Any one (1): Von Neumann uses a **single shared memory (and bus) for both instructions and data**, whereas Harvard uses **separate memories (and buses)** for instructions and data; Harvard can fetch an instruction and access data **simultaneously**, Von Neumann cannot.

**Total: 14 + 6 = 20**
