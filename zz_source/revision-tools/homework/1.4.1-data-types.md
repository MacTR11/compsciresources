# Homework — 1.4.1 Data Types

Name: ______________________   Date: __________   Mark: ______ / 20

*OCR H446 — set after the 1.4.1 lesson. SHOW ALL WORKING.*

---

## Part A — This week's topic (~14 marks)

**1.** Complete the conversion grid. Binary values are 8-bit unsigned. **[3]** *(AO2)*

| Denary | 8-bit binary | Hexadecimal |
|--------|--------------|-------------|
| 156 | (a) | (b) |
| (c) | 11001010 | (d) |
| 111 | (e) | 6F |

@@SPACE:0@@

**2.** Using 8-bit **two's complement**, calculate **52 − 37** by adding the two's complement of 37. Show the binary working and state clearly what happens to the final carry. **[4]** *(AO2)*

**3.** Write **True** or **False** for each statement about shifts and masks. If a statement is false, write a correction in the final column. **[2]** *(AO1/AO2)*

| Statement | True / False | Correction if false |
|-----------|--------------|---------------------|
| Applying an arithmetic right shift of two places to `01001100` gives `00010011`, dividing the value by 4. |  |  |
| ANDing a value with the mask `00001111` sets its four leftmost bits to 1. |  |  |
| Shifting a binary value one place to the left multiplies it by 2 (provided no bit is lost). |  |  |
| XORing a bit with 1 leaves that bit unchanged. |  |  |

@@SPACE:0@@

**4.** Write the letter of the correct definition in the **Match** column next to each term. Each letter is used exactly once. **[3]** *(AO1)*

| Term | Match | Letter | Definition |
|------|-------|--------|------------|
| Character set |  | A | The error that occurs when the result of a calculation is too large to represent in the number of bits available |
| ASCII |  | B | The part of a floating point number that holds its significant digits, determining the precision |
| Unicode |  | C | The complete set of characters a computer can represent, each given a unique binary code |
| Overflow |  | D | A character set with enough codes for the symbols of all the world's major languages |
| Mantissa |  | E | The part of a floating point number that determines where the binary point is placed, giving the range |
| Exponent |  | F | A character set that uses 7 bits, giving 128 codes — enough for the English alphabet, digits and common symbols |

@@SPACE:0@@

**5.** A games developer stores a player's score, which is always a whole number, in a **real (floating point)** variable. Explain, with reference to how integers and real numbers are represented, **one** reason why an **integer** data type is the better choice. **[2]** *(AO1/AO2)*

---

## Part B — Retrieval: keep it fresh (~6 marks)

**6.** *(1.1.1)* State the role of the **MAR** (Memory Address Register) in the fetch–decode–execute cycle. **[1]** *(AO1)*

**7.** *(1.2.1)* Describe how **virtual memory** allows a computer to run a program that is larger than the installed RAM. **[3]** *(AO1)*

**8.** *(1.3.1)* State **one** difference between **lossless** and **lossy** compression, and give a suitable use for each. **[2]** *(AO1)*

---

## Mark scheme

**Part A (14 marks)**

**1. [3]** 1 mark per fully correct row:

| Denary | 8-bit binary | Hexadecimal |
|--------|--------------|-------------|
| 156 | (a) **10011100** | (b) **9C** |
| (c) **202** | 11001010 | (d) **CA** |
| 111 | (e) **01101111** | 6F |

Row 1: 156 = 128 + 16 + 8 + 4 → `10011100`; nibbles `1001` = 9, `1100` = C → `9C`.
Row 2: `11001010` = 128 + 64 + 8 + 2 = 202; nibbles `1100` = C, `1010` = A → `CA`.
Row 3: 6F = (6 × 16) + 15 = 111; 6 = `0110`, F = `1111` → `01101111` (check: 64 + 32 + 8 + 4 + 2 + 1 = 111).

**2. [4]** 52 − 37 = 52 + (−37).
+52 = `00110100` (32 + 16 + 4 = 52). ✓
+37 = `00100101` (32 + 4 + 1 = 37). ✓
−37: invert `00100101` → `11011010`, then + 1 → `11011011`.
Add:

```
  00110100   (52)
+ 11011011   (−37)
-----------
1 00001111
```

The carry out of the most significant bit is **discarded** (the ninth bit is dropped), leaving `00001111` = 8 + 4 + 2 + 1 = **15**. ✓ (52 − 37 = 15)
*Mark: +52 and +37 correct in binary (1); correct two's complement of 37 = `11011011` (1); correct binary addition giving `1 00001111` (1); states the final carry is discarded, giving `00001111` = 15 (1).*

**3. [2]** *(2 correct rows per mark.)*

- Row 1: **True** — `01001100` (76) → `00100110` → `00010011` (19), and 76 ÷ 4 = 19; the sign bit 0 is copied in from the left.
- Row 2: **False** — ANDing with `00001111` **clears the four leftmost bits to 0** (and keeps the four rightmost bits unchanged).
- Row 3: **True**.
- Row 4: **False** — XORing a bit with 1 **flips (inverts)** the bit; it is XORing with 0 that leaves it unchanged.

**4. [3]** Character set = **C**; ASCII = **F**; Unicode = **D**; Overflow = **A**; Mantissa = **B**; Exponent = **E**. *(2 correct matches per mark.)*

**5. [2]** Integers are stored exactly as whole binary numbers, whereas real numbers are stored in floating point as a mantissa and exponent, which can only *approximate* many values (1). So storing the score as a real risks rounding/representation errors (e.g. comparisons failing / the stored value not being exactly the score), and it typically wastes storage and is slower to process — an integer stores every whole-number score exactly (1). *Max 2: one representational point plus one consequence.*

**Part B (6 marks)**

**6. [1]** The MAR holds the **address** of the memory location about to be read from or written to — during fetch, the address of the next instruction copied from the PC (1).

**7. [3]** Virtual memory uses an area of **secondary storage** (a page/swap file) as if it were an extension of RAM (1). When RAM is full, pages of a program not currently in use are moved out to disk, and moved back into RAM when they are needed (1). This allows a program larger than physical RAM to run, although accessing swapped-out pages is much slower than RAM (excessive swapping = disk thrashing) (1).

**8. [2]** Difference (1): lossless compression allows the original file to be reconstructed *exactly*, whereas lossy compression permanently discards some data, so the original cannot be perfectly recovered. Uses (1): lossless for text/source code/program files (or PNG/ZIP), where every bit matters; lossy for photographs/audio/video (e.g. JPEG, MP3), where a small quality loss is acceptable in exchange for a much smaller file.

**Total: 14 + 6 = 20 marks**
