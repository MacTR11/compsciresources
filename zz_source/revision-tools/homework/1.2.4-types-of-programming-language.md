# Homework — 1.2.4 Types of Programming Language

Name: ______________________   Date: __________   Mark: ______ / 20

*OCR H446 — set after the 1.2.4 lesson. Show your working.*

---

## Part A — This week's topic (~14 marks)

**1.** Write the letter of the correct definition in the **Match** column next to each term. Each letter is used exactly once. **[3]** *(AO1)*

| Term | Match | Letter | Definition |
|------|-------|--------|------------|
| Programming paradigm |  | A | A subclass acquires the attributes and methods of a superclass, and may add its own or override them |
| Procedural language |  | B | A style or approach to writing programs, e.g. procedural, object-oriented, assembly |
| Assembly language |  | C | Keeping an object's attributes private so they can only be read or changed through its public methods |
| Class |  | D | A low-level language using mnemonics, where each instruction translates to one machine-code instruction |
| Inheritance |  | E | A template that defines the attributes and methods from which objects are created |
| Encapsulation |  | F | A language built from sequence, selection, iteration and subroutines, setting out step by step *how* a task is done |

@@SPACE:0@@

**2.** The following Little Man Computer (LMC) program inputs two numbers.

```
         INP
         STA  first
         INP
         SUB  first
         BRP  big
         LDA  first
         OUT
         HLT
big      ADD  first
         OUT
         HLT
first    DAT
```

(a) Complete the trace table for the inputs **9** then **5**. Exactly **eight** instructions are executed, and the first row has been done for you. In each row, show the values **after** the instruction finishes (copy a value down if it has not changed; only write in `Output` when a value is output). **[3]** *(AO2)*

| Instruction executed | ACC | first | Output |
|----------------------|-----|-------|--------|
| INP | 9 |  |  |
|   |   |   |   |
|   |   |   |   |
|   |   |   |   |
|   |   |   |   |
|   |   |   |   |
|   |   |   |   |
|   |   |   |   |

@@SPACE:0@@

(b) State the purpose of the program for **any** two inputs. **[1]** *(AO2)*

**3.** A processor executes the instruction `LDA 4`. The relevant contents of memory and registers are:

| Location | Contents |
|----------|----------|
| Address 4 | 7 |
| Address 6 | 9 |
| Address 7 | 2 |
| Index register | 2 |

Complete the table to show the value loaded into the Accumulator when the operand `4` is interpreted using each addressing mode. **[4]** *(AO2)*

| Addressing mode | Value loaded into ACC |
|-----------------|-----------------------|
| Immediate |  |
| Direct |  |
| Indirect |  |
| Indexed |  |

@@SPACE:0@@

**4.** A studio is writing a game containing many types of character (wizards, archers, guards) that all share common behaviour such as moving and holding items. Explain why an **object-oriented** paradigm is well suited to this program. Refer to **inheritance** and **encapsulation** in your answer. **[3]** *(AO2)*

---

## Part B — Retrieval: keep it fresh (~6 marks)

**5.** *(1.2.3)* Write **True** or **False** for each statement about software development methodologies. If a statement is false, write a correction in the final column. **[3]** *(AO1)*

| Statement | True / False | Correction if false |
|-----------|--------------|---------------------|
| In the Waterfall model, the stages are completed in sequence, with each stage finished before the next begins. |  |  |
| In the Waterfall model, the client normally sees working software early and often during development. |  |  |
| Extreme programming is an example of the Waterfall model. |  |  |

@@SPACE:0@@

**6.** *(1.2.2)* Explain the difference between **static** and **dynamic** linking of library code. **[2]** *(AO1)*

**7.** *(1.1.1)* State the purpose of the **Accumulator (ACC)** register. **[1]** *(AO1)*

---

## Mark scheme

**Part A (14 marks)**

**1. [3]** Programming paradigm = **B**; Procedural language = **F**; Assembly language = **D**; Class = **E**; Inheritance = **A**; Encapsulation = **C**. *(2 correct matches per mark.)*

**2 (a) [3]** Completed trace — the branch is **not** taken because the ACC is negative (5 − 9 = −4):

| Instruction executed | ACC | first | Output |
|----------------------|-----|-------|--------|
| INP | 9 |  |  |
| STA first | 9 | 9 |  |
| INP | 5 | 9 |  |
| SUB first | −4 | 9 |  |
| BRP big | −4 | 9 |  |
| LDA first | 9 | 9 |  |
| OUT | 9 | 9 | 9 |
| HLT | 9 | 9 |  |

1 mark: instruction column correct — `BRP big` is followed by `LDA first`, `OUT`, `HLT` (branch not taken). 1 mark: ACC column 9, 5, −4, −4, 9, 9, 9. 1 mark: `first` column shows 9 from `STA first` onwards **and** output **9** on the `OUT` row. (Accept unchanged cells left blank.)

**2 (b) [1]** It outputs the **larger** of the two numbers input (accept: if they are equal it outputs that value) (1).

**3. [4]** 1 mark per row: Immediate = **4** — the operand itself is the value; Direct = **7** — the value held at address 4; Indirect = **2** — address 4 holds 7, which is used as the address, and address 7 holds 2; Indexed = **9** — effective address = operand 4 + index register 2 = address 6, which holds 9.

**4. [3]** Up to 3 from: common behaviour (moving, holding items) is defined once in a superclass such as `Character`, and each character type is a class (1); the wizard/archer/guard subclasses **inherit** this behaviour and add or override their own, so code is not duplicated and the game is easier to maintain and extend with new character types (1); **encapsulation** keeps each object's attributes (e.g. health, position) private, accessible only through its methods, so other parts of the program cannot put a character into an invalid state (1); classes/objects map naturally onto a game made of many similar interacting entities (1). Max 3.

**Part B (6 marks)**

**5. [3]** 1 mark per correct row:

- Row 1: **True**.
- Row 2: **False** — in Waterfall the client normally only sees the product at/near the **end** of the project (early, frequent delivery of working software is a feature of Agile).
- Row 3: **False** — extreme programming is an example of an **Agile** methodology.

**6. [2]** Static linking copies the required library code into the executable at compile/link time, so the executable is self-contained but larger, and library updates require re-linking (1). Dynamic linking keeps the library in a separate file (e.g. a DLL) that is linked/loaded at run time, so the executable is smaller and the library can be shared and updated independently (1).

**7. [1]** The Accumulator temporarily holds the data being processed / the results of arithmetic and logic operations carried out by the ALU (1).

**Total: 14 + 6 = 20 marks**
