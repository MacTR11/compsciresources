# Homework — 1.1.2 Types of Processor

Name: ______________________   Date: __________   Mark: ______ / 20

*OCR H446 — set after the 1.1.2 lesson. Show your working.*

## Part A — This week's topic (~14 marks)

**1.** Complete the comparison table of **RISC** and **CISC** processors by filling in the missing cells. **[3]** *(AO1)*

| Feature | RISC | CISC |
|---|---|---|
| Size of instruction set | Small set of simple instructions | (a) |
| Instruction length | (b) | Variable length |
| Clock cycles per instruction | (c) | Often several cycles |
| Where the complexity lies | (d) | In the hardware/circuitry |
| Program length / RAM needed | (e) | Shorter programs, less RAM needed |
| Typical power consumption | Lower | (f) |

@@SPACE:0@@

**2.** Match each term to its definition by writing the correct **letter** in the Match column. Two definitions are not needed. **[3]** *(AO1)*

| Term | Match |
|---|---|
| Multicore processor |   |
| Parallel processing |   |
| GPU |   |
| Co-processor |   |
| Pipelining |   |
| RISC |   |

| Letter | Definition |
|---|---|
| A | An extra processor that relieves the CPU of specific tasks, e.g. floating-point arithmetic or graphics |
| B | Carrying out several instructions/tasks at the same time |
| C | A processor with a very large number of simpler cores, designed to apply the same operation to many items of data at once |
| D | A CPU containing two or more processing units, each able to fetch, decode and execute its own instructions |
| E | A processor design with a small set of simple, fixed-length instructions that typically execute in one clock cycle |
| F | Overlapping the fetch, decode and execute stages of consecutive instructions |
| G | An architecture in which one shared memory holds both instructions and data |
| H | Small, very fast memory located on or near the CPU |

@@SPACE:0@@

**3.** Each statement in the table below contains **one** factual error about GPUs. Identify the error in each and rewrite the statement correctly on the lines below. **[3]** *(AO1)*

| # | Statement (each contains one error) |
|---|---|
| 1 | A GPU contains a small number of highly complex cores — far fewer than a typical CPU. |
| 2 | A GPU processes graphics efficiently because each core carries out a different instruction on the same single item of data. |
| 3 | GPUs cannot be used for any task other than producing graphics. |

**4.** A smartphone manufacturer uses a **RISC (ARM)** processor rather than a **CISC (x86)** processor. Give **two** reasons, linked to a smartphone, why RISC is the better choice. **[2]** *(AO2)*

**5.** Tick **True** or **False** for each statement. If a statement is false, write a correction in the final column. **[3]** *(AO1/AO2)*

| Statement | True / False | Correction if false |
|---|---|---|
| Any program automatically runs faster on a processor with more cores. |   |   |
| A co-processor system uses an additional processor to take over specific tasks, such as floating-point calculations, from the CPU. |   |   |
| In parallel processing, the computer still completes every instruction one after another, in strict sequence. |   |   |

@@SPACE:0@@

## Part B — Retrieval: keep it fresh (~6 marks)

**6.** Explain why the **Von Neumann** architecture can suffer from a performance **bottleneck**, and state how the **Harvard** architecture avoids it. **[3]** *(1.1.1)*

**7.** State **two** factors (other than number of cores) that affect the performance of a CPU, and for each give its effect. **[2]** *(1.1.1)*

**8.** Name the register that holds the **current instruction** while it is being decoded and executed. **[1]** *(1.1.1)*

---

## Mark scheme

**Part A (14 marks)**

**1. [3]** — 2 correct cells per mark: (a) **large set of complex instructions**; (b) **fixed length**; (c) **usually one cycle**; (d) **in the software/compiler**; (e) **longer programs, more RAM needed**; (f) **higher**.

**2. [3]** — 2 correct rows per mark: Multicore processor = **D**; Parallel processing = **B**; GPU = **C**; Co-processor = **A**; Pipelining = **F**; RISC = **E**. *(G and H are distractors: Von Neumann architecture and cache.)*

**3. [3]** — 1 mark per statement correctly fixed:

- 1: A GPU contains a **very large number of simpler cores** — far **more** than a typical CPU.
- 2: Each core carries out the **same instruction on a different item of data** at the same time.
- 3: GPUs are increasingly used for **general purpose (non-graphics) computing**, e.g. machine learning, scientific modelling/simulation, data mining.

**4. [2]** — 1 mark per valid reason (max 2), linked to a phone: RISC uses **less power** → longer battery life; simpler instructions run efficiently/cool in a small device without a fan; easier to pipeline → good performance per watt; cheaper/simpler hardware keeps the handset cost down. *(Generic "RISC is simpler" with no link caps at 1.)*

**5. [3]** — 1 mark per row (correct verdict; for false statements the correction is required):

| Statement | True / False | Correction |
|---|---|---|
| More cores = automatically faster | **False** | The software must be written to use multiple cores / the task must be capable of being split (parallelised); otherwise extra cores give little or no benefit |
| Co-processor takes over specific tasks | **True** | — |
| Parallel processing is strictly sequential | **False** | In parallel processing several instructions are processed **simultaneously** (strictly one-after-another is serial processing) |

**Part B (6 marks)**

**6. [3]** — 1 mark: Von Neumann uses a **single shared memory and bus** for both data and instructions. 1 mark: so it **cannot fetch an instruction and read/write data at the same time** (they compete for the one bus) — the bottleneck. 1 mark: Harvard uses **separate memories and buses** for data and instructions, so instruction fetch and data access can happen **simultaneously**.

**7. [2]** — 1 mark each (factor + effect), max 2: **clock speed** — more pulses/sec → more FDE cycles/sec; **cache size** — more frequently used data/instructions held near the CPU → fewer slow RAM fetches; **pipelining** — overlaps FDE stages → higher throughput. *(Accept word/bus width.)*

**8. [1]** — **CIR** (Current Instruction Register).

**Total: 14 + 6 = 20**
